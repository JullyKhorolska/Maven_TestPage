package org.example.pages;

import org.openqa.selenium.WebDriver;

public class InitWebDriver {
    public WebDriver webDriver;

    public InitWebDriver(WebDriver webDriver) {
        this.webDriver = webDriver;
    }
}
